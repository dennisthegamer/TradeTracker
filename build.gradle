// Multiloader root — no build logic here.
//
// Shared code lives in common/src/main/ and is compiled into both loader
// subprojects via srcDir (see fabric/build.gradle and neoforge/build.gradle).
// `./gradlew build` produces:
//   fabric/build/libs/tradetracker-fabric-mc<mc>-<version>.jar
//   neoforge/build/libs/tradetracker-neoforge-mc<mc>-<version>.jar
